# 収益化ロードマップ — 公開後にあなたがやること

コードは完成していますが、**収益はここから先の作業で決まります**。
週2〜3時間の運用を前提に、順番にやることをまとめました。

> ⚠️ 大前提: 収益は保証されません。広告収益で月5,000円に届くには目安として
> 月間3〜5万PVが必要で、通常6ヶ月〜1年かかります。届かない可能性もあります。
> 初期(PVが少ない時期)は、AdSenseよりアフィリエイト(1件数百〜数千円)の方が
> 期待値が高いことを覚えておいてください。

---

## STEP 0: 公開する(所要30分・今日やる)

1. **リポジトリ名の変更を推奨**: 現在のリポジトリ名 `-` だと、URLが
   `https://gunmo108.github.io/-/` になります。GitHubのリポジトリ設定
   (Settings → General → Repository name)で `kurashi-dentaku` などに変更すると
   URLの見た目と信頼感が改善します(無料・数クリック)。
   - 変更した場合: `sitemap.xml`・`robots.txt`・各HTMLの `canonical` / `og:url` の
     URL(`/-/` の部分)を新しいリポジトリ名に置換してください。
2. **このブランチをデフォルトブランチにマージ**(またはPRを作成してマージ)。
3. **GitHub Pages を有効化**: Settings → Pages → Source を
   「Deploy from a branch」+ デフォルトブランチ + `/ (root)` に設定。
   数分後に `https://gunmo108.github.io/<リポジトリ名>/` で公開されます。
4. スマホで全ページを開いて動作確認。

## STEP 1: 検索エンジンに登録する(所要30分・公開当日〜翌日)

1. [Google Search Console](https://search.google.com/search-console) に無料登録し、
   サイトを「URLプレフィックス」で追加(所有権確認はHTMLタグ方式が簡単。
   指定された `<meta>` タグを `index.html` の `<head>` に追加)。
2. サイトマップとして `sitemap.xml` のURLを送信。
3. 「URL検査」で主要ページのインデックス登録をリクエスト。

## STEP 2: 計測とお問い合わせを整える(所要1時間・1週目)

1. [Google Analytics (GA4)](https://analytics.google.com/) に無料登録し、測定ID(G-XXXX…)を取得。
   各HTMLの `<head>` にあるGA4のコメントを外し、IDを書き換える。
2. [Googleフォーム](https://docs.google.com/forms)でお問い合わせフォームを作り、
   `contact.html` の手順コメントに従って埋め込む(AdSense審査で問い合わせ手段があると有利)。

## STEP 3: アフィリエイトに登録する(所要1時間・1〜2週目)

**初期の収益源の本命はこちら。** PVが少なくても1件成約すれば数百〜数千円になります。

1. [A8.net](https://www.a8.net/) に無料登録(審査は緩め。サイトURLを登録)。
   - 「電気代」ページ向け: 電力会社切り替え・電気料金比較系の案件
   - 「積立」ページ向け: ネット証券・NISA口座開設系の案件(単価が高い)
2. [もしもアフィリエイト](https://af.moshimo.com/) に無料登録。
   - Amazon・楽天の商品リンク(節電グッズ、電気毛布など記事内で自然に紹介)
3. 提携できたら、各ページ末尾の `<!-- affiliate-slot -->` コメント位置に
   リンクを設置。**「広告」「PR」の表記を必ず付けること**(景品表示法のステマ規制)。

## STEP 4: AdSenseに申請する(公開2〜4週間後)

1. コンテンツが揃い、Search Consoleでインデックスされ始めたタイミングで
   [Google AdSense](https://adsense.google.com/) に申請(無料)。
2. 審査には1日〜2週間かかります。落ちても修正して何度でも再申請できます。
   よくある不合格理由: コンテンツ量不足(→STEP 5でページを増やして再申請)。
3. 合格したら、管理画面で「広告ユニット」を作成し、各ページの
   `<div class="ad-slot">` の中にコードを貼り付け。
4. 収益が **8,000円** に達すると銀行口座に振り込まれます(それまで貯まる)。

## STEP 5: 週2〜3時間の運用(公開後ずっと)

**収益はこのステップの継続量でほぼ決まります。**

### 月2〜3本のペースでツール/記事を追加する

追加候補(検索需要があり、既存の計算ロジックを流用しやすい順):

- ガソリン代・燃費計算(denkidaiの構造を流用)
- 時給換算(月給→時給、残業代)
- うるう年対応の年齢計算・学年早見(hidzukeを流用)
- 睡眠時間計算(何時に寝れば○時間眠れる?)
- 消費税計算(税込⇔税抜)
- 犬・猫の年齢換算
- BMI・適正体重計算
- 育休・産休の期間計算(hidzukeを流用)

### 毎週やること(30分)

1. Search Console の「検索パフォーマンス」を開く
2. **表示回数が多いのにクリック率が低いページ**を見つける
3. そのページの `<title>` と meta description を、実際に検索されている
   言葉に合わせて書き直す(これが最も費用対効果の高い改善です)

### 告知(任意)

- X(Twitter)で「こんなときに使えるツール」の形で各ツールを紹介
- 季節ネタに合わせる: 冬は電気代(こたつ・エアコン)、年末は割り勘、年始は積立

## 現実的なマイルストーン

| 時期 | 目標 | 状態 |
|---|---|---|
| 1ヶ月 | 全ページのインデックス登録・アフィリエイト提携 | 収益ほぼ0円 |
| 3ヶ月 | 月1,000PV・ページ数15本 | アフィリエイトで初収益が出れば上出来 |
| 6ヶ月 | 月1万PV・AdSense合格済み | 月数百〜2,000円 |
| 12ヶ月 | 月3〜5万PV | **月3,000〜10,000円圏内** |

伸びない場合は、Search Consoleのデータを見て「表示されているのに順位が低い」
キーワードにページを寄せていくのが基本戦略です。

---

## 技術メモ

- テスト: `node test/tools.test.js`(計算ロジック)/ `node test/smoke.js`(ゲーム)
- ローカル確認: `python3 -m http.server 8000` → http://localhost:8000
- 計算ロジックは `assets/tools.js` に集約。新ツールを足すときも
  「純関数を追加 → テストを追加 → ページを作る」の順で。
